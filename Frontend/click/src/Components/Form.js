import React from 'react'
import Input from './Input'

const Form = (props) => {
    
    const handleSubmit = () => {
        
    }

    return(
        <form onSubmit={handleSubmit}>

        </form>

    )
}

export default Input