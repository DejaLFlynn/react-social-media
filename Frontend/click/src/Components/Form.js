import React from 'react'
import Form from './Input'

const Form = (props) => {
    
    const handleSubmit = () => {
        
    }

    return(
        <form onSubmit={handleSubmit}>

        </form>

    )
}

export default Form
